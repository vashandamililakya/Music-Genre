services:
  - type: web
    name: music-genre-classifier
    runtime: python
    buildCommand: pip install -r requirements.txt && python train_model.py
    startCommand: gunicorn "main:application" --workers 2 --bind 0.0.0.0:$PORT --timeout 120 --chdir /opt/render/project/src
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_DEBUG
        value: false
