# Core web framework
Flask==3.1.3
Werkzeug==3.1.7
gunicorn>=21.2.0
gunicorn>=21.2.0

# Audio processing
librosa>=0.10.0          # MFCCs, spectral features, beat tracking
soundfile>=0.12.0        # Read/write audio files (WAV, FLAC, OGG)
pydub>=0.25.0            # MP3 support (requires ffmpeg on PATH)

# ML
scikit-learn>=1.4.0      # RandomForest, SVM, preprocessing, metrics
numpy>=1.26.0
scipy>=1.12.0
joblib>=1.3.0            # Model serialization

# Utilities
python-multipart>=0.0.9  # Multipart form parsing
